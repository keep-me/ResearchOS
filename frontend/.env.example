# 前端环境变量配置
# ==========================================
# 说明：开发环境使用，Docker 构建时会被覆盖

# API 基础地址
# 开发环境（默认）：http://localhost:8000
# Docker 生产环境：/api（相对路径，Nginx 代理）
# VITE_API_BASE=http://localhost:8002

# 如果使用 Docker 部署（端口 8002），取消下面这行的注释：
# VITE_API_BASE=http://localhost:8002
